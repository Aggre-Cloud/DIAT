# DIaT runtime dependencies (pinned).
# Install:  pip install -r requirements.txt
#
# This file covers the REQUIRED set — the pipeline refuses to start if any
# of these are missing.  Optional extras (pysbd, ocrmypdf) are listed in
# requirements-optional.txt and are NOT required for normal operation.

openpyxl==3.1.2
pdfplumber==0.11.9
PyPDF2==3.0.1
pypdfium2==5.3.0
googletrans==4.0.0rc1
